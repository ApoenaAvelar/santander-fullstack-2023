spring:
  datasource:
    url: jdbc:h2:mem:sfs2023
    username: sfs2023
    password:
  main:
    allow-bean-definition-overriding: true
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create # validate | update | create | create-drop
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false